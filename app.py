"""
Legacy Streamlit app — use `main.py` instead.

This file is kept for reference. The main application has been moved to main.py
which includes the full RAG pipeline, proper error handling, and Streamlit Cloud
deployment support.

Run:  streamlit run main.py
"""

import streamlit as st

st.warning(
    "This is the legacy entry point. "
    "Please run `streamlit run main.py` instead."
)